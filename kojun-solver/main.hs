-- Trabalho 1 - Main - Kojun Solver

-- Rules:
-- Enter a number in each cell of the grid so that every region of size N contains each number from
-- 1 to N exactly once;
-- Numbers in orthogonally adjacent cells must be different;
-- If two cells are vertically adjacent in the same region, the number in the upper cell must be 
-- greater than the number in the lower cell.

-- main = do 
    -- putStrLn "8x8:"
    -- mapM_ print (resolvedor tabuleiro8x8Valores tabuleiro8x8Grupos)
    -- putStr("\n\n")
    -- putStrLn "10x10:"
    -- mapM_ print (resolvedor tabuleiro10x10Valores tabuleiro10x10Grupos)

